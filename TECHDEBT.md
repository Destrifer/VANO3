# Техдолг

Живой список накопленного техдолга и осознанно отложенных решений. Обновлять по
ходу: закрыли — вычёркиваем (`~~…~~`) или удаляем; появилось новое — дописываем.

Маркеры в коде: `TODO`, `ВРЕМЕННО`, `позже`, `заглушка`, `рыба`.
Последняя ревизия: 2026-06-09.

---

## 1. SEO / pSEO (текущий заход)

- [ ] **Магнитные / металлические кластеры** — не заведены: нет цен на материалы
  (бизнес-данные владельца). Формат: ставка ₽/м² **или** ступени за лист + размер
  заготовки. Блокер закрытия Ф5.
- [ ] **Наполнен только 1 продукт (визитки).** У `business-cards` есть
  `intro_text`/`faq` + 5 кластеров; остальные **29 продуктов** — пустой
  fallback-интро, кластеров нет. → прогонять ядро по направлениям (`seo/PLAYBOOK.md`).
- [ ] **Битые хабы доверия** `/o-nas`, `/guides`, `/garantii`, `/kak-zakazat` —
  страниц нет (ссылки в футере `BaseLayout.astro` + **наши ссылки из интро/FAQ
  визиток на `/guides` ведут в 404**). Near-term: хотя бы заглушка `/guides`.
- [ ] **`urgent`-кластер без пресета** — в калькуляторе нет тумблера срочности
  (движок жёстко `urgent:false`). Это часть Ф6 «срок готовности».
- [ ] **Multipage-конфигуратор не принимает пресет** (`MultipageConfigurator.vue`) —
  кластеры на буклетах/каталогах откроются без преднастройки. Листовой — ок.
- [ ] **Категорийные страницы (хаб L0)** — заглушка «в разработке»
  (`src/pages/[slug].astro`, ветка `category`).
- [ ] **Ф6 не начата:** срок «успеем сегодня» (`urgencyMultiplier` в движке есть,
  нужен расчёт даты), табы галереи, отзывы + `AggregateRating`.
- [ ] **Ф7 не начата:** Tier-2 хвост (по данным GSC), хабы доверия, гайды.
- [ ] **Ручные проверки из чеклиста `06`:** валидация JSON-LD (Rich Results /
  Schema.org), Lighthouse / Core Web Vitals на хабе и кластере.

## 2. Контент-модель (временные решения → перенести в Directus)

- [ ] **`materialType()` парсит тип материала из названия** (`OptionsInfo.astro`,
  помечено «ВРЕМЕННО»). Хрупко: новый материал с нестандартным именем уедет в
  «Прочее». Нужно поле в Directus (`papers.material_type`/`group`).
- [ ] **Картинки материалов/постпечати — единый плейсхолдер**
  (`src/assets/materials/placeholder.jpg`). Реальные — из `papers.image` /
  `finishing_options.image`.
- [ ] **Тексты опций и правила в коде** — `src/lib/optionInfo.ts`,
  `src/lib/pricing/rules.ts` («позже переедет в Directus»).
- [ ] **Срок «1–3 дня» захардкожен** — главная (`index.astro` `lead`) и плашка
  заказа (`OrderPlate.vue` «срок 1–3 раб. дня»). Вынести в настройки/данные.
- [ ] **`presetPrice` round: `ROUND_D=40` хардкод** (`src/lib/pricing/data.ts`) —
  зеркалит дефолт диаметра калькулятора; при смене дефолта будет рассинхрон.

## 3. Инфраструктура / деплой

- [ ] **Применение схемы Directus не автоматизировано.** Схема версионируется
  снапшотом (`directus/snapshot.yaml`), но накат на прод/стейдж — вручную
  (`directus schema apply`). Нужен шаг в деплое.
- [ ] **`DIRECTUS_ADMIN_TOKEN` — dev-токен в `.env`.** На бою — отдельный токен с
  ограниченными правами / процесс ротации.

## 4. Вне SEO (крупное, для контекста)

- [ ] **Корзина → серверный заказ** — заглушки (`CartView.vue`, `lib/checkout.ts`).
  Оплата/доставка — `manual`/`widget`, API провайдеров позже
  (см. `06-delivery-payment.md`).
- [ ] **Превью — «рыба»** (один макет `card`; `Preview.vue`, `BookletPreview.vue`,
  `lib/preview/*`). Реальные макеты/вырубка — позже.

---

## Приоритет (предложение)

1. **Цены магнит/металл** → закрыть Ф5.
2. **Заглушка `/guides`** — наши живые ссылки уже ведут в 404 (прямой SEO-минус).
3. Развилка: **реплицировать ядро на 2–3 продукта** (масштаб) **или** Ф6
   (фишки/доверие).
