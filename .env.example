DIRECTUS_IMAGE=directus/directus:11.5.1
DIRECTUS_PORT=8055
DIRECTUS_PUBLIC_URL=http://localhost:8055
DIRECTUS_CORS_ORIGIN=http://localhost:4321

DIRECTUS_KEY=replace-with-secure-random-key
DIRECTUS_SECRET=replace-with-secure-random-secret
DIRECTUS_ADMIN_EMAIL=admin@example.com
DIRECTUS_ADMIN_PASSWORD=replace-with-secure-admin-password

POSTGRES_DB=directus
POSTGRES_USER=directus
POSTGRES_PASSWORD=replace-with-secure-database-password

# Серверные эндпоинты Astro (/api/*) — токен сервисной роли «Order Service»
# (права: create на orders/order_items/directus_files). Только сервер.
DIRECTUS_URL=http://localhost:8055
DIRECTUS_TOKEN=replace-with-order-service-static-token

# DaData (подсказки адресов) — токен для подсказок, секрет для стандартизации
DADATA_TOKEN=replace-with-dadata-api-token
DADATA_SECRET=replace-with-dadata-secret

# Яндекс.Карты JavaScript API (developer.tech.yandex.ru → «JavaScript API»).
# PUBLIC_ → уходит в браузер (так и нужно для JS API); защищается доменом в кабинете.
PUBLIC_YANDEX_MAPS_KEY=replace-with-yandex-maps-js-api-key

# Уведомления о заявках (форма контактов). Все опциональны — пустой канал пропускается.
# Telegram: создать бота у @BotFather → токен; chat id личного/группового чата (@userinfobot).
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
# Email: SMTP-доступ почтового ящика, куда слать заявки.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
REQUEST_NOTIFY_EMAIL=

# Секрет подписи токена формы (антиспам time-trap + nonce). Любая длинная случайная строка.
FORM_SIGNING_SECRET=replace-with-random-secret
