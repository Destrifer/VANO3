# 06 · Доставка и оплата

Заказ = заявка (см. doc 01; П2 — цена из конфигуратора = цена в счёте). Оплата онлайн — заглушка «скоро».

---

## СТАТУС РЕАЛИЗАЦИИ — читать первым (обновлено 2026-07-16)

**Витрина доставки + модель в Directus СДЕЛАНЫ и на проде.** Ниже — что готово, ключевые файлы,
подводные камни деплоя и что дальше. Разделы после этого блока — исходный проект (часть уже
реализована упрощённо, часть — план на API-фазу).

### Что сделано (2026-07-15/16)

- **Directus `delivery_methods`** (создана через прод-API; в `directus/snapshot.yaml`; **public read
  выдан владельцем**). Поля: `code`, `label`, `type` (pickup/courier/pvz), `logo` (file),
  `price_from` (0=бесплатно, null=«уточнит менеджер», число=ориентир), `price_prefix` («от»/«~»/«»),
  `eta_days` (транзит, раб.дней), `note`, `needs_address`, `free_over_threshold`, `active`, `sort`.
  9 строк: `pickup_main`, `pickup_second`, `courier_msk` (free_over_threshold=true, 790₽),
  `yandex_go`(GO ~650), `dostavista`(~600), `cdek`(от400), `yandex_pvz`(ПВЗ, от320), `5post`(от300),
  `post`(Почта, от250). Метки в плитках короткие (GO/ПВЗ/Почта); лого перевозчиков залиты владельцем.
- **`settings.free_delivery_threshold`** (integer, редактируется в админке, fallback 20000; в снапшоте).
- **Read-слой `src/lib/delivery.ts`**: `getDeliveryMethods`, `methodCost` (авторитет сервера),
  `freeDeliveryProgress`, `isPromoCourier`/`isSelectable`, `findMethod`, `FREE_DELIVERY_DEFAULT`.
- **День доставки** — `leadtime.deliveryDayLabel` (готово + `eta_days`, рабочие дни → «завтра»/пн/…).
- **Общий выбор** — `src/stores/delivery.ts` (`selectedDeliveryCode`, persistent) → плашка пишет,
  корзина предвыбирает.
- **Инъекция данных** — `BaseLayout`: `<script id="delivery-methods">` (JSON) + мета
  `free-delivery-threshold`. Острова читают на маунте (без prop-drilling).
- **Селектор `src/components/DeliverySelect.vue`** (в плашке и в корзине, один компонент):
  самовывоз (офис) строкой; наш курьер — промо-блок с прогресс-баром «добавьте ещё X ₽» до порога /
  выбираемая плитка «✓ бесплатно» после; перевозчики — сетка **3×2** с лого (день недели + цена).
  Свёртка «Получение · Москва ⌄» **только на мобиле** (CSS media query ≥768 всегда развёрнут;
  корзина всегда развёрнута через `startOpen`).
- **`OrderPlate`** — селектор встроен. **`CartView`** — тот же селектор + адрес по способу.
  **`api/order.ts`** — способ/стоимость авторитетно из Directus. **`checkout.ts`** обрезан до оплаты
  (`PAYMENT_METHODS`). **`DeliveryBlock.astro` удалён** (уехал в плашку). **`/dostavka-oplata`** —
  на `delivery_methods` (полные имена ПВЗ через маппинг `code→имя`).
- **Оплата:** «при получении» + «счёт юрлицу» работают; онлайн — заглушка «скоро».
- **Деплой-тулинг:** `ops/deploy.sh` + `npm run deploy`.

### ⚠️ Подводный камень деплоя (важно для новой сессии)

Сайт **SSG**: контент Directus (лого, цены, тексты) запекается на СБОРКЕ. Загрузка лого/правка цены
в админке **сама деплой НЕ запускает** и на живом сайте не видна до ПЕРЕСБОРКИ. Пересобрать:
GitHub → Actions → «Build & Deploy» → Run workflow; или `repository_dispatch: deploy-site`; или
`bash ops/deploy.sh` (нужен `GITHUB_DEPLOY_TOKEN` в `.env`); или любой push кода. Права Directus
(public read) в snapshot НЕ входят — при новой коллекции их надо выдать в админке разово.

### Что дальше (не сделано)

- **Онлайн-оплата** (шлюз ЮKassa/Тинькофф + webhook `payment_status`) — фаза 3, приоритет.
- **Вторая точка самовывоза**: в витрине показан один самовывоз; выбор/уточнение 2-й точки — на
  шаге оформления (не сделано; `pickup_second` в Directus уже есть под это).
- **Курьер по Москве до порога НЕ выбирается** (промо-строка); платный курьер = Яндекс Go/Достависта.
  Если нужно давать выбор курьера за 790₽ до порога — доработать.
- **Авто-пересборка при изменении `delivery_methods`** — расширить Directus Flow `deploy-site`
  (опционально, снимет ручной запуск после правок лого/цен).
- **ПВЗ-виджеты** (карты выбора точки) и **API-адаптеры** (тариф/отправление/трекинг) — фазы 2/4.
- Поля заказа: реализованы `delivery_method`/`delivery_cost`/`delivery_address`/`delivery_data`/
  `payment_*`; `city`/`pvz_code`/`postal_code`/`recipient_*` — по мере надобности.

> Ниже — исходный проект трека. Модель как реализована **упрощена** (type + price_from +
> free_over_threshold + manual), адаптерная схема (`integration`/`cost_rule`/`api`) остаётся
> целью для API-фазы.

---

## Принципы

- **Провайдеро-независимая модель (адаптеры) + конфиг данными в Directus** — как стратегии
  цены (doc 03), макеты превью (doc 02), конвейер скидок. Добавление провайдера ≠ переписывание.
- **Без договоров на старте.** Предлагаем много способов доставки как «выбор + захват
  адреса/ПВЗ», физическую отгрузку делает менеджер в кабинете провайдера. Автоматизация
  (тариф / создание отправления / трекинг) подключается **по одному провайдеру позже**.
- **Сервер — источник истины** (П2): стоимость доставки и статус оплаты считаются/проверяются
  на сервере, клиентским значениям не верим (как и цене товара).

## Оплата

Методы (выбраны владельцем):
1. **Онлайн-предоплата** (карты/СБП) — место в UI зарезервировано, помечено «скоро»
   (кнопка disabled). Реализуется в отдельной фазе.
2. **При получении** — наличные/карта на самовывозе или курьеру (постоплата). Без интеграции.
3. **Счёт для юрлиц** — безнал по счёту (реквизиты), постоплата переводом, ведёт менеджер.

Поля заказа: `payment_method` (online / on_receipt / invoice), `payment_status`
(unpaid / pending / paid / refunded), `payment_id` (id платежа шлюза).

Онлайн-оплата (позже): шлюз **абстрактно** (ЮKassa / Тинькофф — решим при реализации).
Поток: создать платёж в шлюзе (секрет в `.env`) → редирект на оплату →
**webhook** `/api/payment/webhook` (проверка подписи, идемпотентно) → `payment_status=paid`.
Статусу «оплачено» с клиента **не верим** — только из webhook.

B2B-счёт (позже): генерация PDF-счёта с реквизитами, постоплата переводом, подтверждает менеджер.

## Доставка

> **Реализовано — подробности в «СТАТУС РЕАЛИЗАЦИИ» в начале файла.** Кратко: способы — данные
> Directus (`delivery_methods`, `manual`-модель без виджетов/договоров); витрина-селектор
> `DeliverySelect` в плашке товара и корзине (выбор шарится стором); бесплатный курьер по Москве
> от `settings.free_delivery_threshold`, считается авторитетно в `api/order.ts`. Виджеты ПВЗ и
> API-адаптеры — отложены (фазы 2/4).

### Модель

Каждый способ — запись-провайдер (Directus, напр. `delivery_methods`):
- `name`, `type` (`pickup` / `courier` / `pvz` / `postamat`),
- `integration` (`manual` / `widget` / `api`),
- `cost_rule` (`free` / `fixed` / `zone` / `manual` / `api`), `cost`, `zone`, `settings` (JSON),
- `active`, `sort`.

### Режимы интеграции (ключевое для «без договора»)

- **`manual`** — выбор способа + адрес/комментарий; цена `free`/`fixed`/«уточнит менеджер»;
  отгрузка вручную. **Договор не нужен.**
- **`widget`** — встраиваемый виджет выбора ПВЗ (карты СДЭК / Яндекс Маркет / Boxberry);
  захватываем код+адрес точки. **Отображение/выбор обычно без договора**; отгрузка — менеджер
  (manual) или API позже.
- **`api`** — тариф + создание отправления + трекинг через адаптер. **Требует
  регистрации/договора** (у части есть sandbox: СДЭК тест, Borzo/Dostavista песочница).

### Что доступно без договора (на старте)

- ПВЗ-виджеты (карта точек) — встраиваются бесплатно, выбор точки без договора → сохраняем `pvz_code`/`pvz_address`.
- Тариф/отправление/трекинг — нужна регистрация/договор; проверять **по каждому провайдеру**
  (условия меняются).
- **Старт = `manual` + `widget`** (много способов, ноль договоров); `api`-адаптеры — позже.

### Целевые провайдеры

СДЭК, Boxberry, 5Post, Ozon (ПВЗ), Яндекс Маркет (ПВЗ), Яндекс Go (курьер-такси),
Dostavista/Borzo (курьер), постаматы, самовывоз. **Не включаем** формат «мастодонтов»
(Почта России, Деловые Линии) — иной формат и документооборот.

### Стоимость

Считается **на сервере**: `free`=0, `fixed`=ставка из настроек, `api`=из адаптера,
«по запросу»=0/уточнит менеджер. Итог: `goods_total − discount + delivery_cost = total`.

## Поля заказа (`orders`, добавить)

`delivery_method` (→ провайдер), `delivery_cost`, `goods_total`,
`city`, `address`, `postal_code`, `pvz_code`, `pvz_address`,
`recipient_name`/`recipient_phone` (если отличаются),
`payment_method`, `payment_status`, `payment_id`.

## Шаг оформления (checkout, UI)

Расширить форму корзины:
- **Доставка** — выбор провайдера; курьер → адрес; ПВЗ → виджет/выбор точки; показать
  стоимость или «уточнит менеджер».
- **Оплата** — онлайн (disabled, «скоро») / при получении / счёт юрлицу.
- Итог пересчитывается с доставкой (клиент — оценка, сервер — авторитетно при создании заказа).

## Архитектура (адаптер)

```
interface DeliveryProvider {
  quote(dest, order): number | null;     // стоимость (или null = уточнит менеджер)
  createShipment?(order): Promise<...>;   // позже, при наличии API/договора
  track?(id): Promise<...>;               // позже
}
```
Провайдеры = данные в Directus + код-адаптеры по `integration`. MVP: `ManualProvider`
(cost fixed/null). Позже: `CdekProvider`, `YandexProvider`, `BorzoProvider`… — за одним интерфейсом.

## Фазы

1. **Поля заказа + checkout-шаг**: выбор доставки/оплаты, адрес/ПВЗ-виджет, доставка в сумму.
   Оплата: при получении / счёт + онлайн-заглушка «скоро». **Без внешних API.**
2. **ПВЗ-виджеты** (СДЭК/Яндекс Маркет) для выбора точки — без договора.
3. **Онлайн-оплата** (шлюз) + webhook + `payment_status`.
4. **API-адаптеры доставки** per-provider (по мере договоров): тариф/отправление/трекинг.
5. **Счёт для юрлиц** (PDF).
