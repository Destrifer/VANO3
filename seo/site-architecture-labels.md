# Архитектура раздела ЭТИКЕТКИ (ТЗ на страницы)

Ядро: `van2/seo/SEO-ядро_этикетки.xlsx` (749 / 159 222), Tier‑разметка — `..._этикетки_Tier.xlsx`. Реализовано 2026‑06‑15, контент в Directus. Продукт `labels` (id 35), `strategy=sheet`, `production=plotter` (контурная резка любой формы).

> **Решение по архитектуре:** этикетки — **свой хаб `/labels`**, НЕ кластер наклеек. Причины: объём 159k и самостоятельный B2B‑интент «товарная маркировка» (на бутылки/банки, состав, под продукт). Связь с наклейками — **перекрёстные ссылки** (`/labels` ↔ `/stickers`), не вложение.

## Фильтр возможностей
| Можем | НЕ можем (EXCLUDE) |
|---|---|
| Самоклейка (бумага), плёнка (ПП/винил, прозрачная, серебряная), контурная резка, ламинация, фольга | **Рулонные/ленточные**, **флексопечать** (рулонная ротационная), **термотрансфер/термоэтикетки**, принтеры/расходники, **честный знак / маркировка с переменными данными** (решение владельца — не делаем) |

## Распределение спроса (749 запросов)
Tier‑3 (голова → хаб) 104 268 (65%) · Tier‑1 21 378 (13%) · EXCLUDE 17 596 (11%) · Tier‑2 11 771 (7%) · STANDALONE 4 209.

## Хаб `/labels` «Печать этикеток»
H1: **Печать этикеток**. Голова `печать этикеток` 26k + `самоклеящиеся этикетки` 30k. Вхождения Tier‑3: дизайн, цена/тираж, цветность, метод/отделка, **маркировка/переменные данные** (честный знак/штрих/QR). Кросс‑ссылка на `/stickers` (наклейки).

## Tier‑1 — кластеры с плиткой (пресеты в `paperId`/`shape`)
| URL | Материал/форма | Пресет | ~Спрос |
|---|---|---|---|
| `/labels/bottle` | на бутылки/тару (плёнка) | `paperId:21` (Muflon глянц.) | 7 919 |
| `/labels/film` | плёночные/ПП | `paperId:20` (Polylaser глянц.) | 4 375 |
| `/labels/round` | круглые | `shape:round` | 3 325 |
| `/labels/paper` | бумажные | `paperId:17` | 1 907 |
| `/labels/transparent` | прозрачные | `paperId:23` | 1 605 |
| `/labels/metallic` | металлизированные | `paperId:24` | 1 238 |
| `/labels/barcode` | со штрихкодом | `paperId:18` (матовая — скан) | — |
| `/labels/urgent` | срочные | ➖ срочности нет в `CalcPreset` | 1 009 |

> ✅ Дубль `bottle`/`film` устранён 2026-07-11 (`film` → Polylaser глянцевая).
> Материал — `paperId`. Аудит раздела — `cluster-preset-audit.md`.

## Tier‑2 — назначение‑ниши (`show_as_tile=false`, без пресета)
`/labels/wine` вино · `/labels/beer` пиво · `/labels/drinks` напитки/алкоголь · `/labels/cosmetics` косметика · `/labels/food` продукты (масло/мёд/еда) · `/labels/coffee` кофе/чай.

## STANDALONE / EXCLUDE
- STANDALONE (кросс‑продукты): наклейки, бирки, упаковка.
- EXCLUDE: рулонные/ленточные 8 936, флексопечать 5 400, термо 1 861, оборудование, маркетплейс/гео.

## На выверку владельцу
Цены ламинации/фольги и материалов — из каталога (реальные); рыбка в галерее — заменить на фото. **Честный знак / переменные данные — НЕ делаем** (EXCLUDE, подтверждено); кластер «маркировка» не заводим.
