# Аудит пресетов кластеров (антиканибализация)

Каждый кластер (`promoted_pages`) открывает калькулятор ТОГО ЖЕ продукта, что и хаб.
Чтобы страницы не были копиями друг друга, кластер должен задавать **свой пресет** —
стартовое состояние конфигуратора (`preset` JSON, схема — `CalcPreset` в
`src/composables/calcUrlState.ts`).

Снять актуальное состояние с прода: `python seo/_preset_audit.py`
(колонку «Проверен» скрипт не знает — ведём руками: пустой пресет бывает **осознанным**).

## Статусы

- ✅ **проверен** — пресет обдуман; либо задан, либо осознанно оставлен пустым.
- ⬜ **не проверен** — пресет не разбирали.
- ➖ **пресет не нужен** — у кластера нет своей оси в калькуляторе (различие только
  контентное: срочность, «с логотипом», тематика этикетки). Это ✅ с пустым `preset`.

**Важно:** пустой `preset` ≠ ошибка. Ошибка — когда у кластера ЕСТЬ своя ось
(бумага/размер/форма/переплёт/фальцовка), но она не задана.

## Проверенные разделы

### ✅ `/forms` Бланки (sheet) — 2026-07-10

| Кластер | Пресет | Ось |
|---|---|---|
| `letterhead` | `{"sizeIndex":0,"paperId":1}` | А4 + офсетная (головной интент) |
| `self-copy` | `{"paperId":33}` | самокопирующаяся NCR |
| `bso` | `{"sizeIndex":1}` | А5 |

Закрыт долг из `site-architecture-blanks.md` («пресет бумаги самокопирки — на выверку»).
Тогда же включён `products.allow_custom` (плитка «Свой размер») — флаг продукта,
действует и на хабе, и на всех кластерах.

### ✅ `/stickers` Наклейки (sheet) — 2026-07-10

21 кластер: 17 с пресетом, 4 осознанно без. Дублей пресетов нет.
Скрипт (идемпотентный): `seo/_stickers_clusters_presets_faq.py`.

| Кластер | Пресет | Ось |
|---|---|---|
| `paper` | `{"paperId":17}` | самоклейка полуглянцевая |
| `vinyl` | `{"paperId":21}` | Muflon глянцевая |
| `waterproof` | `{"paperId":20}` | Polylaser глянцевая — **был дубль с `vinyl`** |
| `transparent` | `{"paperId":23}` | Muflon прозрачная |
| `reflective` | `{"paperId":25}` | световозвращающая |
| `void` | `{"paperId":29}` | пломбировочная |
| `transfer` | `{"paperId":30}` | переводная |
| `scratch` | `{"paperId":31}` | скретч-слой |
| `equipment` | `{"paperId":26}` | пластик 3M |
| `packaging` | `{"paperId":18,"sizeIndex":2}` | матовая бумага + 100×100 |
| `qr` | `{"paperId":18,"sizeIndex":0}` | матовая (глянец бликует → скан) + 50×50 |
| `round` | `{"shape":"round"}` | круг |
| `figured` | `{"shape":"complex"}` | контурный рез |
| `rectangular` | `{"sizeIndex":1}` | 70×50 (хаб стартует с 50×50) |
| `laminated` | `{"laminationIndex":0}` | ламинация глянцевая |
| `foil` | `{"foil":true}` | фольгирование |
| `small-run` | `{"quantity":50}` | тираж (дефолт 100) |

➖ **Без пресета осознанно:** `urgent` (срочность — не поле `CalcPreset`), `logo`,
`sticker-packs`, `emblems` (различие контентное).

FAQ: у всех 21 кластера ≥3 вопроса (добавлено 37). Исправлен ответ `faq_items` id 44:
обещал опцию скругления углов в конфигураторе, которой у наклеек нет
(finishing = ламинация ×3 + фольгирование); скругление возможно контуром реза.

### ✅ `/labels` Этикетки (sheet) — 2026-07-11

14 кластеров. Устранён дубль `bottle`/`film` (оба были `paperId:21`), заполнен пустой
`barcode`. Дублей пресетов нет.

| Кластер | Пресет | Ось |
|---|---|---|
| `paper` | `{"paperId":17}` | самоклейка |
| `film` | `{"paperId":20}` | Polylaser глянцевая — **был дубль с `bottle`** |
| `bottle` | `{"paperId":21}` | Muflon глянцевая (влагостойкая, на тару) |
| `transparent` | `{"paperId":23}` | Muflon прозрачная |
| `metallic` | `{"paperId":24}` | Oracal серебряная |
| `barcode` | `{"paperId":18}` | матовая самоклейка (глянец бликует → скан) — **был пустой** |
| `round` | `{"shape":"round"}` | круг |

➖ **Без пресета осознанно:** `urgent` (срочность), и Tier-2 назначение-ниши
`wine`/`beer`/`drinks`/`cosmetics`/`food`/`coffee` — своей оси материала нет,
различие контентное; общий пресет-плёнка склонировал бы их друг с другом.

✅ FAQ (2026-07-11): доведено до ≥3 на всех 14 кластерах (+29 вопросов; `barcode`
был с нуля). Дозаливка вложенным M2M-`create` в `promoted_pages` (создаёт
`faq_items` + связку, существующие не трогает). Раздел закрыт полностью.

### ✅ `/photobooks` Фотокниги (multipage) — 2026-07-11

7 кластеров. Было: 6 из 7 с `{}` (без дифференциации), FAQ=0 у всех. Темы МОГУТ
отличаться форматом/переплётом (в отличие от ниш этикеток на одной плёнке) →
пресет их различает, а не клонирует. Мульти-пресет принимает `formatIndex` +
`bindingId` + `pages`. Форматы: 0 Квадрат20 · 1 Квадрат30 · 2 Книжная20×30 ·
3 Альбомная30×20 · 4 А4. Переплёты: 4 Твёрдый 7БЦ · 3 КБС · 2 Пружина.

| Кластер | Пресет | Ось |
|---|---|---|
| `wedding` | `{"formatIndex":1,"bindingId":4}` | квадрат 30×30 + твёрдый (свадебный премиум) |
| `premium` | `{"bindingId":4}` | твёрдый переплёт 7БЦ |
| `corporate` | `{"formatIndex":4,"bindingId":4}` | А4 + твёрдый (деловая) |
| `travel` | `{"formatIndex":3}` | альбомная 30×20 (панорамы) |
| `baby` | `{"formatIndex":0}` | компактный квадрат 20×20 |

➖ **Без пресета осознанно:** `family` (универсальная — дефолт, различие
наполнением), `urgent` (срочность — не ось `CalcPreset`).

✅ FAQ (2026-07-11): +21 вопрос, по 3 на все 7 (все были с нуля). Пресеты и FAQ
залиты одним PATCH на страницу (`seo/photobooks-audit.mjs` — в scratchpad).

### ✅ `/postcards` Открытки (sheet) — 2026-07-11

6 кластеров. Было: `postal` c `{sizeIndex:0}`, остальные 5 пусты, FAQ=0 у всех.

| Кластер | Пресет | Ось |
|---|---|---|
| `designer` | `{"paperId":13}` | дизайнерская Лён 300 (фактурная бумага) |
| `photo` | `{"paperId":11}` | мелованная глянцевая 300 (фотопечать) |
| `logo` | `{"foil":true}` | фольга (акцент на логотипе) |
| `new-year` | `{"foil":true}` | фольга (новогодний акцент) |
| `postal` | `{"sizeIndex":0}` | А6 105×148 (почтовый стандарт) |

➖ **Без пресета осознанно:** `urgent` (срочность — не ось).
⚠️ **Осознанный дубль** `{"foil":true}` у `logo` и `new-year` (выбор владельца):
стартовое состояние калькулятора одинаковое, различие — контентом (H1/интро/FAQ/
тема). Приемлемо как у ниш с общей осью; при желании развести — дать new-year
свой формат/бумагу.

✅ FAQ (2026-07-11): +18 вопросов, по 3 на все 6 (все были с нуля). Скрипт —
`seo/postcards-audit.mjs` (scratchpad).

### ✅ `/tags` Бирки и ярлыки (sheet) — 2026-07-11

5 кластеров, все были пусты, FAQ=0. Ось = размер (спец-форматы) или материал.
Все пресеты различны. Capability — ТОЛЬКО ПЕЧАТЬ (тканые/металл/кожа = EXCLUDE),
поэтому FAQ без обещаний нумерации/переменных данных (capability не решена).

| Кластер | Пресет | Ось |
|---|---|---|
| `luggage` | `{"sizeIndex":4}` | 60×100 багажная (спец-размер) |
| `cable` | `{"sizeIndex":5}` | 25×25 кабельная (спец-размер) |
| `cardboard` | `{"paperId":12}` | Картон 2-сторонний 300 |
| `keys` | `{"paperId":16}` | Пластик ПВХ 0.5 мм (прочность) |
| `clothing` | `{"paperId":13}` | Дизайнерская Лён 300 (навесные ярлыки) |

✅ FAQ (2026-07-11): +15 вопросов, по 3 на все 5. Скрипт — `seo/tags-audit.mjs`.

### ✅ `/badges` Бейджи (sheet) — 2026-07-11

5 кластеров, все пусты, FAQ=0. **Всплыла capability-проблема** (не только пресеты).

| Кластер | Пресет | Ось / решение |
|---|---|---|
| `plastic` | `{"paperId":16}` | Пластик ПВХ 0.5 мм |
| `medical` | `{"laminationIndex":0}` | глянцевая ламинация (протираемый) |
| `metal` | `{"paperId":15}` | **переформулирован** → «Бейджи металлик (под металл)» |

⚠️ **`metal` — capability:** настоящего металла/гравировки НЕТ (вне capability).
По решению владельца **переформулирован в «металлик»**: H1 «Бейджи металлик (под
металл)», tile_label «металлик», интро переписано честно (металлизированная
дизайнерская бумага Металлик 300 + полноцветная печать, НЕ настоящий металл; убрано
обещание гравировки), пресет `paperId:15`.

➖ **Без пресета осознанно:** `magnet` (магнит) и `lanyard` (лента/шнурок) —
крепления, НЕ ось калькулятора (поля «крепление» нет). Владелец подтвердил, что
оба крепления предлагаются → кластеры остаются, FAQ описывает крепление как
аксессуар к печатному бейджу.

✅ FAQ (2026-07-11): +15 вопросов, по 3 на все 5. FAQ без обещаний настоящего
металла; крепление = сверление/еврослот + аксессуар. Скрипт — `seo/badges-audit.mjs`.

### ✅ `/graduation-albums` Выпускные альбомы (multipage) — 2026-07-11

4 кластера по ступеням, все пусты, FAQ=0. Ступень НЕ определяет формат/переплёт
(различие контентное), но по решению владельца — мягкие дефолты формата под
конвенцию ступени. Все пресеты различны.

| Кластер | Пресет | Ось (конвенция) |
|---|---|---|
| `kindergarten` | `{"formatIndex":3}` | квадрат 20×20 (детсад) |
| `grade-4` | `{"formatIndex":1}` | книжная 20×30 |
| `grade-9` | `{"formatIndex":2}` | альбомная 30×20 |
| `grade-11` | `{"formatIndex":0,"bindingId":4}` | А4 + твёрдый (премиум-выпуск) |

✅ FAQ (2026-07-11): +12 вопросов, по 3 на все 4. Скрипт — `seo/gradalbums-audit.mjs`.

### ✅ `/safety-signs` Знаки безопасности (sheet) — 2026-07-11

4 кластера, все пусты, FAQ=0. Материалов всего 2 (фотолюм. плёнка id34 / бумага
id35, ГОСТ 12.4.026), отделка ламинация. Ось = размер/материал; категория знака
(пожарный/предупреждающий) — контент, не калькулятор.

| Кластер | Пресет | Ось |
|---|---|---|
| `evacuation` | `{"sizeIndex":4,"paperId":34}` | Эвакуационный 300×150 + плёнка |
| `navigation` | `{"paperId":34}` | фотолюм. плёнка (долговечная навигация) |

➖ **Без пресета осознанно:** `fire`, `warning` — различие категорией знака
(пиктограммой), а не производством: материал и квадратные размеры общие.
FAQ по ГОСТ, без обещаний жёстких/металлических знаков (только фотолюм. самоклейка).

✅ FAQ (2026-07-11): +12 вопросов, по 3 на все 4. Скрипт — `seo/safetysigns-audit.mjs`.

### ✅ `/blueprints` Чертежи (sheet) — 2026-07-11

4 кластера, все пусты, FAQ=0. Оси бедные: 2 размера (А4/А3 — крупнее вне
capability, через партнёров), материал по факту офсетная 80 (уже дефолт).

| Кластер | Пресет | Ось |
|---|---|---|
| `a3` | `{"sizeIndex":1}` | размер А3 |

➖ **Без пресета осознанно:** `copy` (копирование), `folding` (фальцовка),
`project-docs` (комплект документации) — это СЕРВИСНЫЕ формулировки, а не
калькуляторная ось (все = А4 офсетная, пресет-ключа нет). FAQ по capability:
печать А4/А3 (крупнее — партнёры по запросу), копирование = печать с файла
(скан НЕ делаем), фальцовка А3→А4 есть.

✅ FAQ (2026-07-11): +12 вопросов, по 3 на все 4. Скрипт — `seo/blueprints-audit.mjs`.

### ✅ `/books` Книги (multipage) — 2026-07-11

7 кластеров, FAQ=0 у всех. Устранён **дубль** `{bindingId:4}` (`hardcover`+`leather`)
и **capability**-проблема (кожаного переплёта нет).

| Кластер | Пресет | Ось |
|---|---|---|
| `hardcover` | `{"bindingId":4}` | твёрдый 7БЦ |
| `leather` → «Подарочные книги» | `{"formatIndex":1,"bindingId":4}` | А4 + твёрдый (**переформулирован**) |
| `softcover` | `{"bindingId":3}` | КБС (мягкий) |
| `single-copy` | `{"quantity":1}` | 1 экземпляр |
| `small-batch` | `{"quantity":10}` | малый тираж |
| `children` | `{"formatIndex":1}` | А4 (детский формат) |

⚠️ **`leather` — capability + дубль:** кожаного переплёта/обложки в продукте НЕТ
(переплёты Скрепка/Пружина/КБС/7БЦ, обложки только мелованные). Обещал «кожаный
переплёт, золочение обреза, тиснение». Переформулирован в **«Подарочные книги»**
(H1/плитка/интро: твёрдый 7БЦ + А4 + фольга, честно без кожи), пресет
`{formatIndex:1,bindingId:4}` — это заодно **развело дубль** с `hardcover`.

➖ **Без пресета осознанно:** `urgent` (срочность — не ось).

✅ FAQ (2026-07-11): +21 вопрос, по 3 на все 7. Скрипт — `seo/books-audit.mjs`.

### ✅ `/planners` Ежедневники (multipage) — 2026-07-11

4 кластера, FAQ=1 у каждого. **Две capability-переформулировки** (кожа/гравировка).
Обложки только мелованные, отделка ламинация + фольга; кожи/кожзама/лазера НЕТ.

| Кластер | Пресет | Ось / решение |
|---|---|---|
| `logo` | `{"foil":true}` | логотип фольгой |
| `leather` → «Подарочные ежедневники» | `{"bindingId":4}` | твёрдый 7БЦ + фольга (**переформулирован**) |

⚠️ **`leather`** обещал кожзам/софт-тач/тиснение/гравировку → **«Подарочные
ежедневники»** (твёрдый 7БЦ + фольга, честно без кожи). ⚠️ **`engraving`** обещал
лазерную гравировку по кожзаму → **«Именные ежедневники»** (персонализация имени
ФОЛЬГОЙ, не лазер). H1/плитка/интро переписаны; стале-FAQ (гравировка/кожа/УФ-лак/
ляссе у logo) заменены, осиротевшие `faq_items` удалены.

➖ **Без пресета осознанно:** `dated` (датированность — свойство блока, не ось),
`engraving` (пусто, чтобы не дублировать `logo` foil — различие контентом: имя vs логотип).

✅ FAQ (2026-07-11): +11 вопросов, доведено до 3 на всех 4 (стале-вопросы у 3
заменены). Скрипт — `seo/planners-audit.mjs`.

### ✅ `/magazines` Журналы (multipage) — 2026-07-11

4 кластера, FAQ=0. **Потребовало правки кода:** мультипейдж-пресет не умел бумагу
обложки/блока → добавлены ключи `coverPaperId`/`innerPaperId` в `CalcPreset` +
`applyMultipagePreset` (коммит 5d1922a). Теперь `glossy` получил настоящую ось.

| Кластер | Пресет | Ось |
|---|---|---|
| `glossy` | `{"coverPaperId":11}` | глянцевая обложка (глянц. блока НЕТ у продукта) |
| `newspapers` | `{"formatIndex":2}` | А3 (газетный разворот) |
| `corporate` | `{"foil":true}` | фольга-логотип (дефолт владельца отложен → выбран foil) |

➖ **Без пресета осознанно:** `urgent` (срочность — не ось).

✅ FAQ (2026-07-11): +12 вопросов, по 3 на все 4. Скрипт — `seo/magazines-audit.mjs`.

> **Новая возможность для будущих multipage-кластеров:** ось-бумага теперь
> пресетится (`coverPaperId`/`innerPaperId`). Раньше приходилось оставлять пустым.

## Очередь на проверку

Снимок на 2026-07-10: **137 кластеров / 29 продуктов, 83 пустых пресета.**
Крупные первыми (потенциал канибализации выше там, где кластеров много).

| Продукт | Стратегия | Пустые пресеты | Статус |
|---|---|---|---|
| `/pos-materials` POS-материалы | sheet | wobblers, pricetags, hangers | ⬜ |
| `/docs` Печать документов | sheet | presentations, color, binding | ⬜ |
| `/folders` Папки | fixed | logo, diecut | ⬜ |
| `/invites` Приглашения | sheet | wedding, birthday | ⬜ |
| `/certificates` Сертификаты | sheet | gift, relief | ⬜ |
| `/tickets` Билеты | sheet | lottery, numbered | ⬜ |
| `/business-cards` Визитки | sheet | urgent | ⬜ |
| `/brochures` Брошюры | multipage | urgent | ⬜ |
| `/booklets` Буклеты | sheet | urgent | ⬜ |
| `/catalogs` Каталоги | multipage | urgent | ⬜ |
| `/notebooks` Блокноты | multipage | logo | ⬜ |
| `/copybooks` Тетради | multipage | logo | ⬜ |
| `/menus` Меню | sheet | folder | ⬜ |
| `/envelopes` Конверты | fixed | logo | ⬜ |
| `/diplomas` Дипломы | sheet | thesis | ⬜ |
| `/evacuation-plans` Планы эвакуации | sheet | development | ⬜ |

**Гипотеза для быстрых решений:** `urgent` (10 шт. в разных продуктах) и `logo` —
скорее всего ➖ (нет своей оси, различие в контенте/сроке). Проверить и отметить,
не выдумывая пресет ради пресета.

## Как проверять раздел

1. `python seo/_preset_audit.py` — увидеть пустые.
2. Достать оси продукта (размеры/бумаги/переплёты в порядке индексов):
   `GET /items/products/<id>?fields=sizes.*,papers.papers_id.*,bindings.bindings_id.*`
   — индексы пресета считаются по этому порядку.
3. Сопоставить интент кластера (из `site-architecture-<раздел>.md`) с осью.
4. `PATCH /items/promoted_pages/<id> {"preset": {...}}` — правка контента сама
   триггерит пересборку (Flow «Deploy site on publish»).
5. Проверить в HTML живой страницы: `curl -s https://printmos.ru/<product>/<cluster> | grep -o 'preset&quot;:\[0,{[^]]*'`
6. Отметить раздел здесь ✅ с таблицей пресетов.

## Движок: что учтено

- ✅ `presetPrice()` учитывает `preset.sizeIndex` (коммит `34fb391`) — плитка хаба
  показывает цену своего размера, а не `sizes[0]`.
- ✅ Материалы `status=draft` не попадают в калькулятор и в цену «от» (коммит `59d7114`).
- ✅ **Материал в пресете задаём `paperId`, а не `paperIndex`** (там же). Индекс —
  позиция в M2M: пересортировка материалов молча ломала пресеты. Все пресеты в базе
  мигрированы на `paperId` (проверено 2026-07-10, `paperIndex` не осталось).
  `paperIndex` поддерживается только как legacy-вход.
- ⚠️ `sizeIndex`/`laminationIndex`/`foldTypeIndex` **остаются индексами** — при
  пересортировке размеров/отделки у продукта перепроверить его кластеры.
- ⚠️ `price=0.00` у материала — **не всегда ошибка**: при заполненном `fixed_price`
  (штучные ступени) это норма. См. память `pricing-fixed-price-materials`.
