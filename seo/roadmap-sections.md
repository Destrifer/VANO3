# Роадмап: оставшиеся разделы (TODO)

**Статус на 2026-06-16.**

✅ **Готово (хаб + кластеры/контент в Directus):** визитки, наклейки (+3D `volume-stickers`), брошюры, буклеты (+листовки/флаеры), каталоги, меню, этикетки, **конверты, папки, блокноты (+склейка), ежедневники** — 12 разделов.

▶️ **Где остановились / следующее:** идём по **Wave 1**. Закрыты конверты/папки/блокноты/ежедневники. **Дальше по Wave 1:** упаковка, события/награды, таблички/знаки (каждый — по приходу ядра от владельца). Затем Wave 2 (многостраничные) и Wave 3 (мелочь).

📦 **Артефакты:** ядра с Tier — `van2/seo/SEO-ядро_<раздел>_Tier.xlsx`; архитектура/механизмы — `seo/site-architecture-*.md` + `06-seo-implementation.md`; процесс — `PLAYBOOK.md`. Контент разделов — в Directus (вне гита).

> **Типы калькулятора по разделам:** sheet (визитки/наклейки/этикетки/меню/буклеты), multipage (брошюры/каталоги), **fixed «тип+тираж→цена»** (конверты/папки/блокноты/ежедневники, 3D). Цены fixed/материалов — **черновые**, на выверку владельцем.

## Процесс на каждый раздел (повторяемый цикл)
1. **Владелец:** экспорт ядра из Я.Вебмастера (регион Москва) → `van2/seo/SEO-ядро_<раздел>.xlsx`.
2. **Claude:** Tier-разметка + чистка мусора (Шаг 4.5 PLAYBOOK) → `..._Tier.xlsx`.
3. Архитектура: хаб + кластеры (Tier-1 плитки / Tier-2 без плитки) + вхождения (Tier-3).
4. **Конфиг продукта:** часто пустой/не того типа — донастроить (размеры/материалы/finishing; `multipage` для книг/журналов; см. как делали catalogs/menus/labels).
5. Хаб + кластеры + рыбка-плейсхолдер → сборка/превью (проверять пресет ПОСЛЕ гидрации).
6. Цены/факты — черновик по конкурентам, пометка на выверку.

> Малые STANDALONE без выраженных модификаторов можно собрать **из caps без ядра** (один хаб, без кластеров) — но ядро всё равно полезно для вхождений/чистки.

---

## Wave 1 — ценные, своя голова (нужны ядра от владельца)
- [x] **Конверты** (`envelopes`) — fixed, хаб + кластер `logo`. ✅
- [x] **Папки** (`folders`) — fixed (типы), хаб + кластеры logo/rings/address/plastic/diecut. ✅
- [x] **Блокноты** (`notebooks`) — fixed (типы), хаб + кластеры logo/spiral. `notepads` (склейка) снят с публикации (тип внутри notebooks). ✅
- [x] **Ежедневники** (`planners`) — fixed (типы), хаб + кластеры logo/engraving/leather/dated. ✅
- [ ] **Упаковка** (`packaging-stickers` → возможно отдельный продукт «упаковка») — крафт-пакеты/коробки, если делаем.
- [ ] **События/награды** (`invites`, `cards`, `postcards`, `certificates`, `diplomas`, `awards`, `gratitudes`, `tickets`) — общий заход: каждый свой хаб ИЛИ группа с кросс-ссылками; сезонный спрос.
- [ ] **Таблички и знаки** (`signs`, `info-signs`, `direction-signs`, `safety-signs`, `evacuation-plans`, `badges`, `emblems`) — B2B; «таблички на дверь», «знаки безопасности», «бейджи».

## Wave 2 — многостраничные (переиспользуют механизм брошюр/каталогов)
- [ ] **Книги** (`books`), **Журналы** (`magazines`), **Фотокниги** (`photobooks`), **Тетради** (`copybooks`) — настроить как `multipage` (переплёт/полосы), зеркало brochures. Ниша, ниже приоритет.

## Wave 3 — мелочь STANDALONE (низкий приоритет; можно из caps)
- [ ] POS: `pricetags` (ценники), `tags` (бирки), `hang-tags` (ярлыки), `hangers` (хенгеры), `wobblers` (воблеры), `stencils` (трафареты).
- [ ] `barcodes` / `barcode-stickers` (штрихкоды), `equipment-stickers` (на оборудование).
- [ ] `coupons` (купоны), `presentations` (презентации), `forms` (бланки), `docs` (документация), `blueprints` (чертежи), `maps-atlases` (карты).

## Отдельные треки
- [ ] **Календари** — продукта НЕТ; завести + **свой калькулятор** (квартальные/настенные/карманные/домики). Крупный сезонный. По решению владельца — позже.
- [ ] **Буквы/цифры** (плоттерная резка) — допуслуга; нужна **модель цены** (по высоте/размеру/комплекту), затем отдельная страница.

## Исключено (не делаем)
- **Широкоформат:** `banners`, `posters`, `placards`, `rollups`, `canvas` — нет оборудования. Рекомендация: **снять с публикации** пустые продукты (или оставить пустыми).
- **Этикетки:** рулонные/ленточные, флексопечать, термотрансфер, **честный знак/переменные данные** — EXCLUDE.
- Авто-наклейки, DTF/УФ-перенос, голографические, магнитные наклейки — EXCLUDE.

## Долги по уже сделанному
- [ ] Выверить **черновые цены**: материалы наклеек (void/переводная/скретч), брекеты 3D; размеры брошюр/этикеток.
- [ ] Заменить **рыбку-плейсхолдер** на реальные фото в галереях (владелец зальёт).
- [ ] Опубликовать/проверить визиточные Tier-2 (ламинация/УФ-лак/VIP — статус draft/published).
