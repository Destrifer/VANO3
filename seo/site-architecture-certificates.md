# Архитектура раздела СЕРТИФИКАТЫ (ТЗ на страницы)

Ядро: `van2/seo/SEO-ядро_сертификаты.xlsx` (291 / 9 828), Tier — `..._сертификаты_Tier.xlsx`. Wave 1 («События и награды»). Продукт `certificates` (id 15), `strategy=sheet`.

> **Решение:** свой хаб `/certificates` (голова «печать сертификатов» 2 487, подарочные 1 051). Кросс‑ссылки ↔ `/awards` (грамоты), `/diplomas`, `/gratitudes`, `/photobooks` (сертификат на фотокнигу).
> ⚠️ **«Тату сертификат» (3 882) исключён** — это потребительский интент «купить подарочный сертификат тату‑салона», без интента печати; страница‑мисматч. На выверку владельцу (если тату‑салоны — целевой B2B на печать их сертификатов, можно вернуть как нишу).

## Распределение (291 / 9 828)
| Tier | n | спрос | доля |
|---|---|---|---|
| Tier‑3 (хаб) | 113 | 2 596 | 26.4% |
| `/certificates/gift` (подарочные) | 43 | 1 396 | 14.2% |
| `/certificates/relief` (рельеф/тиснение, Tier‑2) | 12 | 140 | 1.4% |
| STANDALONE (→ грамоты/фотокниги/дипломы) | 28 | 953 | 9.7% |
| EXCLUDE | 95 | 4 743 | 48.3% |

EXCLUDE: **тату‑сертификат 3 882** (потребит.), копицентр/«распечатать документы» 405, бренды 385 (Чехов‑типография, аврора принт, нетпринт), шаблоны/DIY 36, мем «антидвойка» 34. _(Без «тату» EXCLUDE ~9%.)_

## Хаб `/certificates` «Печать сертификатов»
H1: **Печать сертификатов**. Голова: `печать сертификатов` 725, +москва/изготовление/на заказ. Вхождения Tier‑3: печать/изготовление сертификатов, **именные**, **качества**, бланки сертификатов, нумерация, размер сертификата (интекст), на плотной/дизайнерской бумаге, А4, цена, срочно. Кросс‑ссылки: `/awards`, `/diplomas`, `/gratitudes`, `/photobooks`.

## Tier‑1 — плитка
| URL | Интент | Пресет | ~Спрос |
|---|---|---|---|
| `/certificates/gift` | **подарочные сертификаты** (на заказ, с конвертом, именные) | `{}` (контент) | 1 396 |

## Tier‑2 — без плитки
`/certificates/relief` — **рельефная печать / тиснение / конгрев / УФ**, прозрачные и пластиковые сертификаты (премиум‑отделка, ~140).

## STANDALONE / EXCLUDE
- STANDALONE: грамоты (→ `/awards`), дипломы (→ `/diplomas`), сертификат на фотокнигу/печать фото (→ `/photobooks`).
- EXCLUDE: **«тату сертификат»** (потребит. покупка, не печать — на выверку), копицентры/«распечатать документы», бренды‑конкуренты, шаблоны/«пустые»/«скачать», мем «антидвойка», офсет.

## На выверку
- **Тату‑сертификат** (3 882) — вернуть как нишу `/certificates/tattoo` только если делаем печать подарочных сертификатов для тату‑салонов (B2B). Сейчас EXCLUDE.
- Форматы (А4/А5), бумаги (дизайнерская, сертификат‑бумага), тиснение/фольга — черновые; галерея — рыбка.
