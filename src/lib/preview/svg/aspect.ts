// Пропорция куклы на плитке там, где каталожный размер НЕ равен готовому изделию.
//
// Плитка берёт формат из `sizes[0]` продукта — и это верно почти всегда. Но у
// части изделий в каталоге заведён размер ЗАГОТОВКИ: у папок это 440×320 мм,
// то есть развёртка под вырубку, а готовая папка — портретная A4 220×310. В
// плитке из-за этого стоял ландшафтный прямоугольник, в котором папку было не
// узнать.
//
// Почему таблица здесь, а не в `mockups.ts`: в калькуляторе пропорцию задаёт
// РЕАЛЬНЫЙ выбор пользователя (он покупает лист 440×320, и видеть должен его),
// а «каталожный размер — это развёртка» — проблема витрины. Тот же принцип, что
// у `overrides.ts`: компромиссы носителя не протекают в реестр сцен.
//
// Правило добавления: сюда попадает продукт, у которого печатный формат и
// готовое изделие — РАЗНЫЕ вещи, а не тот, чья пропорция просто не нравится.
export const tileAspect: Record<string, { w: number; h: number }> = {
  // папка в готовом (сложенном) виде — A4 плюс поле на корешок
  folder: { w: 220, h: 310 },
};
