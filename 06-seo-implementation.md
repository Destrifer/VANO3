# 06 · SEO — техническая реализация

Стратегия — в доке 05 (что и почему). Здесь — **как реализуем в коде**: что сейчас отсутствует и какими файлами закрывается. Принцип-ограничитель (05 §36, §11): не плодить тонкие однотипные страницы. Сначала довести флагман (**визитки**) до глубины, проверить на сборке, потом реплицировать шаблон.

## Текущий разрыв (факт)

Стратегия зрелая, но почти не реализована. Страница услуги (`src/pages/[slug].astro`, ветка `product`) — конфигуратор + заглушка из двух предложений (`SEO-текст — статика (позже из Directus)`, строки 124–139). На сайте **нет**: JSON-LD (ни одного из четырёх требуемых в 02 §20), `canonical`, Open Graph / Twitter, `sitemap.xml`, `robots.txt`, хлебных крошек. `BaseLayout` (`src/layouts/BaseLayout.astro:29-37`) отдаёт в `<head>` только `title` + `description`.

Заход режем по приоритету: (A) технический фундамент сайтово → (B) одна услуга по полной анатомии как шаблон. Контент — в **Directus**.

**Доп. гэпы (сверка кода с архитектурой `seo/site-architecture-vizitki.md`):** ~~(1) конфигуратор не принимает пресет/URL~~ — ✅ **сделано** (`calcUrlState.ts`: `applyPreset`/`initUrlSync`, подключено в `SheetConfigurator`/`ProductConfigurator`); ~~(3) нет SEO/контент-полей~~ — ✅ **сделано** (Ф2: `faq_items` + поля `products`, визитки наполнены). Осталось: (2) на хабе **нет блока плиток** (`SegmentTiles` кормится пустым списком) — это и есть незакрытая часть Ф5. Целевой масштаб раздела «визитки» — **9 базовых страниц** (хаб + 8 плиток), до ~13 с Tier-2.

---

## Архитектура: хаб-модель и тематическая сетка

Это смысловой каркас, на который ложатся Этапы 3–4. Реверс-инжиниринг лидера ниши (coral-print, ~670 URL) показал работающую схему — переносим её, исправляя их ошибки.

### Ядро = матрица «продукт × оси модификации»

Под-страницы строятся не из ключей по одному, а по **осям модификации продукта**, и эти оси = **опции конфигуратора / производства** (значит, тянутся из Directus):

| Ось | Источник в Directus | Примеры значений (визитки) |
|---|---|---|
| Постпечать/отделка | `finishing_options` | ламинация (глянец/мат), фольгирование, тиснение, скругление |
| Материал | группы `papers` | дизайнерская бумага, пластик, картон |
| Форма | `sizes`/`allow_round`/`allow_complex` | круглые, сложной формы |
| Тир | (контент) | VIP/премиум |
| Аудитория | (внешняя ось) | для ресторанов, для врачей |
| Гео | (внешняя ось) | по району/метро — **только при реальном присутствии** |

Матрица `продукт × значение_оси` = список **кандидатов**, НЕ план. В страницы превращаются только ячейки, прошедшие триангуляцию + 3 фильтра (05 §49, §77). Полный декартов перебор = дорвей-спам (05 §36). Соответствие модели: `promoted_pages.предустановка_опций` — это **одна ячейка матрицы** (продукт с предвыбранной опцией + свой контент). Бонус: **sitemap конкурента — готовый валидированный список осей/значений**, сам по себе источник триангуляции.

### Сетка = дерево + латеральные связи

Каждый узел — индексируемая страница; связи плотные:

```
Категория (хаб L0)  → вниз: список услуг
   ↓
Услуга/Продукт (хаб L1)  ↑ вверх: крошки   ↓ вниз: карточки сегментов с ценой «от»
   ↓
Под-интент (L2 = promoted_page)  ↕ вбок: siblings (фольга ↔ тиснение ↔ дизайн. бумага)
   → инфо↔коммерция: гайды/блог (обе стороны)   → под-кластер: примеры работ, отзывы
```

Плотность сетки — сигнал тематической глубины и рост авторитетности без закупки ссылок (05 §33).

### Решение по URL

Иерархию несут **крошки + перелинковка**, не глубина пути (Google не требует вложенности URL). → Оставляем **плоские слаги** (текущий `[slug].astro`), хабность строим связями. Вложенные пути (`[...path].astro`) — опционально, не приоритет.

### Что значит «сделать страницу хабом» (порядок работ)

Сейчас `/vizitki` — лист. Хабом его делают три связи: **вниз** (блок карточек под-интентов с ценами «от» → `promoted_pages`), **вверх** (крошки, Этап 1), **вбок** (смежные услуги + гайды). Отсюда: каркас хаба (место под карточки сегментов) закладываем в **Этапе 3**, наполняем листьями в **Этапе 4**. Категория (`type: "category"`) — хаб L0 над продуктами.

---

## Этап 1 — Технический фундамент (сайтово)

Дешёвые победы, разблокируют остальное. Источник бизнес-разметки — `getSettings()` (`src/lib/settings.ts`: `company_name, phone, email, address, hours, map_lat/lng`).

1. **`astro.config.mjs`** — `site: "https://printmos.ru"` (абсолютные canonical + sitemap). Интеграция `@astrojs/sitemap` с фильтром служебных маршрутов (`/cart`, `/api/`).
2. **`public/robots.txt`** — разрешить обход, `Sitemap: https://printmos.ru/sitemap-index.xml`.
3. **JSON-LD хелперы** — новый `src/lib/seo/jsonld.ts`: чистые билдеры `organization()`, `localBusiness(settings)`, `breadcrumbList(items)`, `productOffer({name, lowPrice, url})`, `faqPage(items)`. + компонент `src/components/seo/JsonLd.astro` (`<script type="application/ld+json">`). Разметка типобезопасная, в одном месте.
4. **`src/layouts/BaseLayout.astro`** — расширить `Astro.props`: `canonical?`, `ogImage?`, `noindex?`, `jsonLd?` (массив) + `<slot name="head">`. В `<head>` добавить:
   - `<link rel="canonical">` (по умолчанию `new URL(Astro.url.pathname, Astro.site)`; pSEO позже передаёт свой — 05 §60);
   - Open Graph (`og:title/description/type/url/image`, `og:locale=ru_RU`) + Twitter Card (`summary_large_image`);
   - глобальный `LocalBusiness`/`Organization` JSON-LD из `settings` — на всех страницах (брендовый + локальный, 02 §27);
   - дефолтную OG-картинку в `public/`.
5. **Хлебные крошки** — новый `src/components/Breadcrumbs.astro`: видимая навигация (Главная → Категория → Услуга) + `BreadcrumbList` JSON-LD. В ветках `category` и `product` в `[slug].astro` (`parentName/parentSlug` уже в props).
6. **Speculation Rules** (02 §18, дешёвое преимущество) — `<script type="speculationrules">` в `BaseLayout` на префетч вероятного перехода (категория → товар).

---

## Этап 2 — Модель контента в Directus

«Тянет / хранит» как в доке 04: цены тянутся из каталога; текст/FAQ/шаблон страница хранит у себя (того, чего нет на родителе — иначе страница не нужна, 05 §51).

1. **`products`** — добавить поля: `meta_title`, `meta_description`, `intro_text` (rich), `characteristics` (форматы, бумаги, постпечать, MOQ, сроки — **числами**), `template_file` (file — скачиваемый шаблон макета), `faq` (M2M → `faq_items`).
2. **`faq_items`** (04 §48) — `question`, `answer`.
3. **Код доступа** — расширить `Product` type/запрос `fields=` в `src/lib/products.ts`; `getServiceContent(slug)` (новый `src/lib/services.ts` либо расширить `pricing/data.ts`): мета + контент + FAQ + URL шаблона (через `assetUrl()`). Все поля **опциональны** → старые продукты не ломаются (graceful fallback, как в `works.ts`).

> Схема Directus — работающий сервис; поля добавляются в админке/через API отдельно от правок репозитория. Код пишем устойчивым к пустым полям.

---

## Этап 3 — Страница услуги: флагман «визитки»

Переписать ветку `props.type === "product"` в `src/pages/[slug].astro` по анатомии 05 §19 (сверху вниз). Сделать и проверить на `/visitki` → это шаблон для остальных услуг.

1. **SERP-слой** — `pageTitle` из `meta_title` (≤60 знаков, с зацепкой: срок / «от» цены / «в Москве»), `pageDescription` из `meta_description`; фолбэк на текущий генерат, если пусто.
2. **H1** — дословно по запросу (`h1`/`name`), не слоган.
3. **Первый экран** — что это + цена «от» + вход в конфигуратор. Цену «от» брать готовой функцией `defaultPrice()` (`src/lib/pricing/data.ts:125`) — тот же движок, что на главной (единый источник, П2).
4. **Конфигуратор** — остров, уже есть (`ProductConfigurator.vue`).
5. **Характеристики текстом** — главный SEO-актив + материал для AI-цитирования: форматы, бумаги, постпечать, мин. тираж, сроки конкретными числами (из `characteristics`). Дублирует возможности конфигуратора текстом (для машины и на случай нерендера острова).
6. **Примеры именно этого продукта** — `getWorks({ product: slug })` уже есть (`src/lib/works.ts`), `Gallery.astro` подключена.
7. **Требования к макету + скачиваемый шаблон** (`template_file`) + ссылка на гайд (заглушка-ссылка; страница гайда — вне захода).
8. **FAQ** — рендер из `faq` + `FAQPage` JSON-LD (билдер из этапа 1). **Опционален**: гейт `content.faq.length > 0` (заполнено → блок + schema, пусто → ничего). Цель — вопросный хвост + AEO, **не** rich result (Google отключил FAQ-сниппеты с мая 2026; schema оставляем под AI/Яндекс). Наполнение — выдача/Wordstat/подсказки + НЧ-вхождения в ответы (05 «Откуда брать FAQ»).
9. **Внутренние ссылки** — крошки вверх (категория), вбок на смежные услуги той же категории, на гайды. Тематическая сетка (05 §33).
10. **JSON-LD страницы** — `Product` + `Offer` (цена «от», `priceCurrency: RUB`, `availability`), `BreadcrumbList`, `FAQPage` — через проп `jsonLd` в `BaseLayout`.
11. **Блок плиток (сегменты хаба)** — новый компонент (напр. `src/components/SegmentTiles.astro`): карточки-ссылки на кластерные страницы (Tier-1) с ценой **«от X ₽» по пресету** (через `defaultPrice()` с предустановкой опции). Список плиток — связь продукта с его `promoted_pages` (Этап 4) или явный список. До создания кластеров — плитки скрыты/заглушки. Это и делает страницу хабом (связь «вниз»).
12. **Вхождения (Tier-3) на хабе** — синонимы головы (изготовление/заказ/производство/типография визитки), цена/недорого, тираж 100/1000/опт, **USP-блок доставки** (спрос ~3.6k) + онлайн-заказ — текстом/блоками, НЕ отдельными страницами (детали — `seo/site-architecture-vizitki.md`).

После валидации визиток — реплицировать шаблон на остальные продукты (Directus наполняется фактами; ИИ оформляет факты владельца, 05 §88–103). Следующая итерация.

---

## Этап 4 — pSEO (после базовых услуг)

Стратегия — 05 §43–65, модель данных — 04 §68–98. Здесь — как реализуем. **Запускается только после Этапов 1–3**: pSEO-страница переиспользует анатомию услуги и ссылается на родителя, поэтому базовый шаблон должен существовать раньше (совпадает с «посевным запуском», 05 §81).

### Общий паттерн (контекст)

Шаблон + набор данных → много URL: одна раскладка, N строк-данных, каждая строка = отдельная страница со своим URL/мета/H1/контентом и **self-canonical**. Два режима генерации: **build-time SSG** (на сборке HTML на строку — наш случай) и on-demand ISR (для миллионов волатильных страниц — избыточно). Граница «pSEO vs спам» (05 §49) — не объём автоматики, а **перебор без пользы**: генерим из явного `published`-списка, не из декартова произведения опций. Три фильтра на строку: спрос + уникальный контент + коммерческий интент.

### Предусловия

1. ~~Конфигуратор не принимает пресет и не читает URL~~ — ✅ **СДЕЛАНО.** Логика в `src/composables/calcUrlState.ts`: `applyPreset(calc, preset)` (семантический пресет `{shape,sides,quantity,paperIndex,foil,foilColorIndex,laminationIndex}` с защитой диапазонов), `presetFromSearch`/`searchFromCalc` (короткие читаемые параметры `?foil=1&paper=…&qty=…`), `applyPresetFromUrl` (один раз читает URL на клиенте), `buildShareUrl` (собирает ссылку на расчёт). Подключено: `ProductConfigurator` принимает проп `preset` → `SheetConfigurator` применяет в setup (работает в SSR-рендере острова) + `applyPresetFromUrl` в `onMounted`. **Важно по SEO:** URL **НЕ** синхронизируется автоматически — параметры появляются только по кнопке «Получить ссылку на расчёт» (`OrderPlate`-зона), чтобы в обычном обходе не плодились параметрические URL. Кластерный пресет приходит с сервера по **чистому** URL. Проверено: смена опций не меняет адрес; кнопка копирует `…?foil=1&lam=2`; расшаренная ссылка открывает конфигуратор с пресетом. ⚠️ Реализовано для **листового** конфигуратора; многостраничный (`MultipageConfigurator`) пресет пока не принимает (нужно при кластерах на multipage-продуктах).
2. **Базовый шаблон услуги (Этап 3) готов и наполнен** хотя бы на флагмане — pSEO-страница его расширяет. ✅ визитки наполнены (Ф2).

### Реализация по шагам

1. **`src/lib/promotedPages.ts`** (новый) — `getPromotedPages()` (фильтр `status=published`, все поля) и `getPromotedPage(slug)`. По образцу `products.ts`/`works.ts`, graceful fallback.
2. **Маршрут — в тот же `src/pages/[slug].astro`.** Сейчас `getStaticPaths` отдаёт `category` + `product`; добавляем третий тип `promoted`:
   ```
   getStaticPaths → [...categoryPaths, ...productPaths, ...promotedPaths]
   ```
   Это правильнее отдельного роута: продвигаемая страница — вариант страницы услуги (тот же конфигуратор, та же анатомия), со своим контентом и пресетом. Slug'и плоские и общие → **гарантия уникальности** (валидация в Directus) + порядок приоритета на случай коллизии.
3. **Ветка `type === "promoted"`** рендерит ту же анатомию услуги, но:
   - H1/title/meta — из полей самой страницы (`h1`, `meta_title`, `meta_description`);
   - `intro_text`, `галерея`, `faq` — свои;
   - **`canonical` на саму себя**, НЕ на родителя (05 §60 — иначе самодедупликация);
   - конфигуратор открывается с `предустановка_опций` (через предусловие 1);
   - внутренние ссылки родитель ↔ под-вариант («Визитки» ↔ «Визитки с фольгированием») — иерархия + анти-каннибализация (04 §97).
4. **JSON-LD/мета** — тот же набор, что у услуги (`Product/Offer`, `BreadcrumbList`, `FAQPage`), но из полей promoted-страницы.
5. **Sitemap** подхватит автоматически — это обычные статические маршруты.
6. **Контент** — процесс 05 §88: ИИ оформляет реальные факты владельца, ручная модерация фактов. Нечем наполнить (своя галерея/специфика/FAQ) — страницы нет.

### Анти-каннибализация (на этапе отбора, не кода)

Сгруппировать синонимы в один интент **до** подсчёта страниц («фольгирование» = «тиснение фольгой» = «золотой/серебряной фольгой» → одна страница, формулировки внутри в секциях/FAQ), 05 §79. Объём найденных формулировок ≠ объём страниц.

### Критические файлы (Этап 4)

| Файл | Что |
|------|-----|
| `src/composables/useCalculator.ts` | приём `initialConfig` + синхронизация с URL (предусловие) |
| `src/lib/promotedPages.ts` *(новый)* | чтение `promoted_pages (published)` |
| `src/pages/[slug].astro` | третий тип путей `promoted` + ветка рендера, self-canonical |
| `src/components/ProductConfigurator.vue` | проброс пресета/URL-состояния в конфигуратор |

---

## Вне этого захода (зафиксировано)

- **pSEO** — вынесено в Этап 4 выше (после базовых услуг, требует доработки конфигуратора под пресет/URL).
- **Хаб/доверие** (`/o-nas`, `/guides`, `/garantii`, `/kak-zakazat`) — сейчас битые ссылки в футере (`BaseLayout.astro:178-180`); страницы делаем отдельно.

---

## Конкурентный бенчмарк (coral-print.ru)

Лидер ниши, проверен вручную (главная + robots/sitemap + 4 внутренних страницы). Образец работающей хаб-модели и одновременно — карта их упущений.

**Профиль:** домен с 2009 (≈17 лет), 3 офиса у метро, ~670 URL программных страниц, конструктор визиток, таблицы цен по тиражам, отзывы по продуктам.

**Копируем (работает):**
- дерево под-интентов по осям модификации (технология, постпечать, материал, тир, форма) — напр. `…/laminaciya-vizitok/glyancevye-vizitki.html`;
- иерархические хабы-крошки, где каждый узел — отдельная страница (тематическая сетка);
- глубокий хаб-лист услуги (`/vizitki` ≈2000 слов: таблица цен по тиражам, карточки сегментов, сроки, бумага, отзывы);
- инфо-блог (`/blog/chto-takoe-*`) со ссылками в коммерческие страницы;
- под-кластер отзывов по продуктам (`/reviews/businessCards`);
- гигиена `robots.txt` (Disallow пагинации/поиска/корзины, `Clean-param` для Яндекса, конструктор вне индекса).

**Делаем лучше (их дыры — наша фора):**
- у них **нет JSON-LD нигде** (ни Product/Offer, ни LocalBusiness, ни BreadcrumbList) при готовых крошках/ценах/3 локациях/отзывах → наш Этап 1 даёт rich-сниппеты, за которые они не борются;
- нет meta description и OG на проверенных страницах;
- **нет FAQ-блоков нигде** → наш FAQ ловит вопросительный хвост и идёт в AI-ответы (фора уже **не** про rich-сниппет — его Google убрал с мая 2026 — а про покрытие в AEO/Яндексе и поведенческие);
- в sitemap нет `lastmod`.

**Избегаем:**
- тонкие **гео-дорвеи** по метро/городам без реального присутствия (`…/vizitki-belorusskaia`, `…-v-lipecke`) — уникален ~1 абзац, остальное шаблон; анти-паттерн 05 §37. Гео — только при настоящей дифференциации.

---

## Порядок реализации (roadmap)

Фазы с зависимостями. Внутри фазы — мелкие шаги/коммиты (AGENTS: малые шаги, теория → практика).

| Фаза | Что | Зависит от | Зачем в этом порядке |
|---|---|---|---|
| **1. Тех-фундамент** (Этап 1) | site+sitemap, robots, JSON-LD хелперы, head BaseLayout (canonical/OG/LocalBusiness), крошки, speculation rules | — | сайтово, дёшево, польза всем страницам сразу; ничего не блокирует |
| **2. Контент-модель** (Этап 2) | поля `products` (meta/intro/characteristics/template/faq), `faq_items`, `getServiceContent()` | — | нужно, чтобы хабу было откуда брать контент |
| **3. Хаб «визитки»** (Этап 3, п.1–10, 12) | анатомия страницы услуги + вхождения + крошки + Product/FAQ/Breadcrumb JSON-LD | Ф1, Ф2 | глубокая посадочная-флагман; шаблон для остального |
| **4. Конфигуратор: пресет + URL** | `useCalculator` приём `initialConfig` + sync с URL; проброс в `ProductConfigurator` | — (можно параллельно Ф1–3) | **блокер кластеров**; нужен и для «расшарить расчёт» (02 §92) |
| **5. Кластерные страницы Tier-1** (Этап 4) | `promoted_pages` + генерация в `[slug].astro` + ветка `promoted` + self-canonical + пресет; **блок плиток на хабе** (Этап 3 п.11) | Ф3 (шаблон), Ф4 (пресет) | посевные 3–5 флагманов: тиснение/фольга, пластиковые, срочные, дизайнерская бумага |
| **6. Фишки-дифференциация** | срок готовности «успеем сегодня» (срочные; `urgencyMultiplier` в движке есть, нужен расчёт даты — 02 §63), галерея-табы, отзывы + `AggregateRating` | Ф5 | усиливают пост-клик опыт (NavBoost) после того, как страницы стоят |
| **7. Хвост и хабы доверия** | Tier-2 страницы (по данным GSC), `/o-nas`/`/guides`/`/garantii`/`/kak-zakazat`, гайды | Ф5 + данные | расширяем на данных своего сайта (П7), не на догадках |

**Контрольная точка после Ф5:** опубликовать, подождать 2–4 нед., открыть GSC/Вебмастер → следующую волну Tier-1/Tier-2 отбирать на реальных показах (П7).

**Минимальный «вертикальный срез» для первой проверки гипотезы:** Ф1 + Ф2 + Ф3 (хаб визиток вживую с schema/контентом) — уже даёт измеримый результат до того, как браться за кластеры (Ф4–5).

### Статус реализации (на 2026-06-09)

| Фаза | Статус |
|---|---|
| Ф1 Тех-фундамент | ✅ сделано (jsonld/JsonLd, Breadcrumbs, site+sitemap, robots) |
| Ф2 Контент-модель | ✅ сделано (Directus: `faq_items` + поля `products`; визитки наполнены; snapshot в `directus/snapshot.yaml`) |
| Ф3 Хаб визиток | ✅ сделано: анатомия + контент + интро(2 кол.) + материалы + FAQ + **плитки** (`SegmentTiles` из кластеров продукта) |
| Ф4 Конфигуратор: пресет+URL | ✅ сделано (`calcUrlState.ts`; пресет с сервера, ссылка по кнопке; листовой конфигуратор, multipage — нет) |
| Ф5 Кластеры Tier-1 | ✅ срез готов: коллекция `promoted_pages` (+M2M `faq`) в Directus; `src/lib/promotedPages.ts`; **отдельный роут `src/pages/[product]/[cluster].astro`** (вложенный URL `/business-cards/foil`, self-canonical, пресет в конфигуратор, крошки 4 уровня); плитки на хабе. Засеяны 3 кластера визиток: `foil` (пресет фольги), `urgent`, `designer`. Осталось: пресеты для urgent/designer, цена «от» на плитках, остальные кластеры из `site-architecture`. |
| Ф6 Фишки, Ф7 Хвост/хабы доверия | ❌ нет |

> **Решение по URL кластеров:** выбран **вложенный** путь `/<product>/<cluster>` (отдельный роут `[product]/[cluster].astro`), а не плоский слаг — иерархия читается в адресе, совпадает с внутренними ссылками. Это пересматривает раннее «оставляем плоские слаги» из §51.

---

## Критические файлы

| Файл | Что |
|------|-----|
| `astro.config.mjs` | `site` + sitemap |
| `src/layouts/BaseLayout.astro` | head: canonical, OG, глобальный LocalBusiness, speculation rules, новые props |
| `src/pages/[slug].astro` | ветка product → полная анатомия (этап 3), крошки в обе ветки |
| `src/lib/seo/jsonld.ts` *(новый)* + `src/components/seo/JsonLd.astro` *(новый)* | разметка Schema.org |
| `src/components/Breadcrumbs.astro` *(новый)* | крошки + BreadcrumbList |
| `src/components/SegmentTiles.astro` *(новый)* | блок плиток хаба (карточки кластеров «от X ₽») |
| `src/composables/useCalculator.ts` | пресет (`initialConfig`) + sync с URL — блокер кластеров |
| `src/lib/promotedPages.ts` *(новый)* | чтение `promoted_pages` (кластерные страницы Tier-1) |
| `src/lib/products.ts`, `src/lib/services.ts` *(новый/расширить data.ts)* | чтение новых полей |
| `src/lib/works.ts`, `src/components/Gallery.astro` | переиспользуются как есть |
| `src/lib/settings.ts` | источник LocalBusiness (без изменений) |
| `public/robots.txt`, дефолтная OG-картинка | новые |

## Проверка (end-to-end)

1. `npm run build` — без ошибок; в выводе у `/visitki`: `<link rel=canonical>`, OG-теги, JSON-LD (`Product/Offer`, `BreadcrumbList`, `FAQPage`, глобальный `LocalBusiness`), текст характеристик и FAQ (не заглушка).
2. `sitemap-index.xml` сгенерирован и содержит услуги; `robots.txt` ссылается на него.
3. dev → `/visitki`: проверить H1/характеристики/FAQ как реальный текст; первый экран с ценой «от» и конфигуратором.
4. JSON-LD через валидатор Schema.org / Rich Results (ручной шаг).
5. Пустые Directus-поля → страница не падает (fallback).
6. Lighthouse/CWV на `/visitki` — LCP/CLS/INP в зелёной зоне (02 §13).
